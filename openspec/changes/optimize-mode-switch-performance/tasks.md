## 1. 切换状态与诊断基线

- [x] 1.1 在运行时/事务边界定义可关联的模式切换阶段、结果和脱敏耗时事件，并用单元测试验证成功、预检失败、启动失败与回滚事件顺序和不含凭据。
- [x] 1.2 为桌面控制器增加单一后台模式切换作业及不可阻塞的快照读取接口，验证重复模式、代理、规则和退出请求在作业期间被禁用或明确拒绝。
- [ ] 1.3 将主窗口和托盘接入进行中阶段、最终结果及实际 `applied_mode`，验证等待就绪时窗口仍可移动、最小化、切换到日志页并读取状态。
- [x] 1.4 移除模式控件旁“切换模式可能中断现有 SSH”等静态预警文案，验证规则、全局和直连模式页面均不显示该提示，且不改变实际模式或错误反馈。

## 2. 无中断预检与受控重启

- [x] 2.1 抽取非直连目标的可复用预检产物，完成凭据读取、上游解析、配置编译、受限写入和内核校验，并用测试验证预检失败不会停止当前有效内核或改变已应用模式。
- [x] 2.2 让启动阶段消费预检产物，保持现有成功提交、持久化和失败回滚语义；运行 `cargo test --locked` 验证配置、事务和控制器覆盖通过。
- [x] 2.3 为预检临时文件和失败路径实现受限清理与启动残留处理，验证文件权限、错误日志和诊断输出均不含明文凭据。
- [x] 2.4 统一 Windows 配置预检、内核启动、TUN 清理和 DNS 刷新的隐藏子进程创建方式；移除状态页切换进度横幅与操作 ID，保留禁用控件、托盘阶段提示和失败错误反馈。

## 3. 代理模式内核热切换与恢复边界

- [x] 3.1 扩展非直连内核配置以声明 Rule、Global、Direct 路由分支和仅监听回环地址的受认证控制接口；验证配置检查通过、运行配置和诊断不暴露接口密钥。
- [x] 3.2 实现规则代理与全局代理间的受认证模式 API 更新与读取确认，验证成功切换保持同一 core PID、TUN 适配器和网络接管，且不调用停止、启动、适配器删除或 Windows DNS 刷新。
- [x] 3.3 实现接口认证、连接、HTTP 结果、未知模式及确认不一致的失败处理，验证失败保持此前实际模式、持久模式和运行内核，不回退为重启。
- [x] 3.4 保留全局直连、退出和异常恢复时的完整内核停止、适配器删除、路由/DNS 恢复和 DNS 缓存刷新，验证 DNS 刷新失败不会虚报恢复成功且可重试。

## 4. Windows 实机验收与交付

- [ ] 4.1 在 Windows x64 已解锁桌面会话使用有效 SOCKS5/HTTP fixture，采集规则->全局、全局->规则热切换与代理->直连的阶段耗时；验证两种热切换均保持相同 core PID、TUN 与 DNS 接管，并保存脱敏日志和截图证据。
- [ ] 4.2 在 Windows 验证无效代理、内核启动失败、本机接口认证失败、接口超时、模式确认不一致和 DNS 刷新失败，确认旧实际模式保持或恢复、错误可读、托盘与主窗口一致且不会并发执行第二次切换。
- [ ] 4.3 在 Windows 默认尺寸及 760×520、100% 与 150% 缩放下，验证切换进行中控件状态、日志可读性和托盘响应；保存验收截图。
- [x] 4.4 运行 `cargo fmt --check`、`cargo test --locked`、`cargo clippy --locked -- -D warnings`、Windows x64 release 构建和 `openspec validate optimize-mode-switch-performance --strict`，记录结果及二进制 SHA-256。

验证记录（2026-09-22）：`cargo fmt --check`、`cargo test --locked`（104 tests）、`cargo clippy --locked -- -D warnings`、两个变更的严格 OpenSpec 校验与 `git diff --check` 均通过。使用 `cargo-xwin` 构建 Windows x64 release；`socks-proxy.exe` SHA-256 为 `0069e9b39c86d1de409ceac7426871d93175602cbab054884abb220f9411fb40`，已部署到 Windows 验证主机并由 `Get-FileHash` 核对一致。
