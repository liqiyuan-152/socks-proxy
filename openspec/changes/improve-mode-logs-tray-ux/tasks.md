## 1. 默认模式与启动状态

- [x] 1.1 将新配置的默认期望模式改为规则代理，并更新存储测试，验证无配置首次加载保留 `desired_mode=Rules`、不开机启动且不生成无效代理或凭据。
- [x] 1.2 为无当前代理的首次恢复增加待配置路径，验证内核、TUN、路由和 DNS 均不修改，界面不显示 Running；保存首个有效当前代理后按 Rules 尝试应用。
- [x] 1.3 更新首次启动、备份导入和恢复相关测试及验证程序，确认已有配置继续恢复其持久模式，导入仍保持已恢复的 Direct 事务约束。

## 2. 模式切换免确认

- [x] 2.1 调整模式切换控制器接口，使用户选择模式时自动允许必要的 Restart 计划，同时保留底层验证、提交、回滚和其他配置修改的确认能力；用单元测试覆盖 Hot、Restart、应用失败和回滚失败。
- [x] 2.2 移除桌面与托盘模式切换的确认弹窗路径，在模式控件附近展示连接可能中断说明；验证单击 Rules、Global 和 Direct 各触发一次切换且不会留下待确认状态。
- [x] 2.3 更新 SSH/模式切换验证程序，确认真实内核重启无需二次确认、旧 SSH 可按已声明边界断开、新连接使用新模式，失败仍恢复旧有效状态。

## 3. 完整日志详情

- [x] 3.1 为连接日志实现紧凑摘要与逐条完整详情，完整展示时间、目标、端口、出站、规则、结果和换行错误文本；在最小窗口宽度验证长目标与长错误可完整读取和选择复制。
- [x] 3.2 增加日志详情的展示测试，覆盖成功、未知规则、长失败原因、脱敏内容和清空后关闭详情，确认不读取或暴露原始未脱敏内核文本。

## 4. 托盘菜单可靠性

- [x] 4.1 将稳定菜单 ID 到命令的解析提取为可测试逻辑，覆盖三种模式、动态代理、五个页面、退出及未知 ID，验证所有启用菜单项都有处理结果。
- [x] 4.2 统一托盘命令执行反馈：页面入口显示并聚焦对应页面，模式/代理成功后同步选中状态，失败时显示窗口和错误，退出失败保留可重试界面；增加控制器与 UI 状态测试。
- [x] 4.3 实现单一后台退出状态机：立即显示进度、禁用冲突操作、忽略重复退出，完成后关闭、失败后恢复界面；用确定性测试验证 UI 线程不执行阻塞清理且每次只启动一个退出事务。
- [x] 4.4 为内核停止、PowerShell/`pnputil` TUN 清理和 `ipconfig /flushdns` 增加集中定义的超时与失败归因，验证超时终止受管进程树、保留恢复记录并允许幂等重试，不在清理未完成时关闭应用。
- [ ] 4.5 在 Windows 10/11 真实桌面会话逐项点击右键菜单，记录模式、代理、五个页面和退出的可观察结果；测量退出进度首次可见时间与清理总耗时，并注入清理超时确认错误和重试入口。

## 5. 文档、构建与交付

- [x] 5.1 更新用户指南和验证文档，说明首次默认 Rules、无代理待配置状态、模式切换免确认但可能断连、日志完整详情、托盘错误反馈以及退出进度/超时重试；检查文档不再声称首次 Direct 或模式切换需确认。
- [x] 5.2 运行 `cargo test --locked`、`cargo fmt --check`、严格 Clippy、Windows x64 release 构建和 `openspec validate improve-mode-logs-tray-ux --strict`，全部通过后记录结果。
- [x] 5.3 重新制作安装包并在 Windows 10/11 验证首次启动、升级安装、托盘与日志交互、响应式退出及卸载网络清理，记录应用/安装器 SHA-256 和机器可读证据。
