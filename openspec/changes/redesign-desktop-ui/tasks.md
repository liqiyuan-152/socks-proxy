## 1. 实施基线与界面基础

- [x] 1.1 在 `improve-mode-logs-tray-ux` 完成后核对最终 `UiState`、日志详情和退出状态接口，记录本变更复用的行为并运行现有 UI/控制器测试确认基线通过。
- [x] 1.2 将集中在 `src/ui/mod.rs` 的代码拆分为主题、通用组件和五个页面模块，保持公开 UI 类型及控制器调用语义不变，并用编译与现有测试验证拆分无行为回归。
- [x] 1.3 建立字体、颜色、间距、尺寸、圆角、描边和状态色令牌，接入本地 Lucide 图标资源及许可证，验证资源离线加载失败时关键命令仍有文字或可访问名称。
- [x] 1.4 实现页面标题、工具栏、分段选择、状态标记、数据行、空状态、错误横幅、表单行和图标按钮组件，用组件测试覆盖正常、悬停、焦点、禁用和错误状态。

## 2. 应用框架与全局反馈

- [x] 2.1 重做常规宽度侧栏和窄窗口图标栏，保持五页顺序、唯一选中态与工具提示，并用布局测试验证响应式断点前后尺寸稳定。
- [x] 2.2 实现持续可见的全局状态区，统一显示模式、当前代理、运行阶段和网络接管状态，用状态快照测试覆盖直连、运行、应用中、退出中及异常。
- [x] 2.3 统一耗时操作、成功、失败和重试反馈，确保冲突控件禁用、错误完整可读且不会伪造成功；用确定性测试验证重复提交和控制器失败路径。
- [x] 2.4 调整默认窗口至约 1000×680 并保留 760×520 最小尺寸，验证窗口缩放期间导航、状态区、对话框和内容滚动不重叠或抖动。

## 3. 状态与代理页面

- [x] 3.1 重做状态页，将模式分段选择、当前代理、运行摘要、最近错误和最近连接按清晰优先级排列，用快照状态测试覆盖有代理、无代理、运行和异常场景。
- [x] 3.2 重做代理管理为紧凑列表，突出当前代理、协议、服务器、认证和行级操作；验证长 IPv6/域名、空列表、选择、编辑和删除均有可读结果。
- [x] 3.3 将代理编辑器迁移到统一表单与对话框组件，验证字段错误靠近输入项、密码不进入调试文本或截图证据，并保持 DPAPI 存储路径不变。

## 4. 规则页面

- [x] 4.1 重做规则管理列表与启用控件，稳定展示名称、目标、端口、备注和操作；验证最小窗口下低优先列进入详情而非永久丢失。
- [x] 4.2 增加按规则名称、目标或备注的即时筛选，验证大小写处理、组合文本、无结果与清除筛选不会修改规则数据或顺序。
- [x] 4.3 将规则编辑器迁移到统一表单与对话框组件，验证域名、IP、CIDR、范围和端口错误清晰可见，保存、取消和确认流程保持原有事务语义。

## 5. 日志与设置页面

- [x] 5.1 在 `improve-mode-logs-tray-ux` 的摘要加详情基础上重做日志页面，提供目标文本、出站和结果筛选；验证筛选组合、清空及记录移除会同步清理失效详情选择。
- [x] 5.2 完善日志长文本、复制、横向约束和结果状态显示，验证长目标、长规则、长错误及脱敏样本在 760px 窗口可完整读取且原始内核文本不会进入 UI。
- [x] 5.3 重做设置页分组和导入预览，使启动、备份与网络恢复状态层级明确；验证导入错误、缺少凭据、非 Direct 禁止导入及文件操作失败路径。

## 6. 可用性、文档与交付

- [ ] 6.1 完成键盘焦点顺序、Enter/Space 操作、图标工具提示和非颜色状态表达，用 Windows 实机逐页验证键盘操作与鼠标结果一致。
- [ ] 6.2 在 Windows 10/11 的 100% 和 150% 缩放下，以默认尺寸和 760×520 最小尺寸检查五页、编辑器和关键运行状态，保存正常、空、应用中、长文本、错误及退出中截图证据。
- [x] 6.3 更新用户指南和原生布局验证文档，说明新导航、筛选、详情、反馈和最小窗口行为，并移除旧布局尺寸或旧截图描述。
- [x] 6.4 运行 `cargo fmt --check`、`cargo test --locked`、严格 Clippy、Windows x64 release 构建和 `openspec validate redesign-desktop-ui --strict`，记录通过结果并确认 DNS、回滚、凭据和网络恢复测试未回归。
- [ ] 6.5 重新制作 Windows 安装包，完成升级安装、首次启动、托盘联动、窗口隐藏/恢复、响应式退出和卸载网络清理回归，记录二进制与安装器 SHA-256。
